name: Kinsta CI Course Project

on:
  push:
    branches: [ "main" ]
  
permissions:
  contents: read

jobs:

  php-tests:
    runs-on: ubuntu-latest
    
    steps: 
    - name: checkout
    uses: actions/checkout@v3
    - name: Test code for errors
    run: php -1 index.php
    
